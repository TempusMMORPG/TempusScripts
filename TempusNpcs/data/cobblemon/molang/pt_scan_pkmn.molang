'Finds Pokemon of matching name in party and scans it';
t.party_pokemon = q.player.party.find_by_properties(q.npc.config.scan_requested);
t.party_pokemon != 0 ? {
    q.player.tell('§a§l[✔] ' + q.npc.name + '§a§l scanned your ' + q.npc.config.scan_requested_display + ' [✔]§r');
} : return 0;