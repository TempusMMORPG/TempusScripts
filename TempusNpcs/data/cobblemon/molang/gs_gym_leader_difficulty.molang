'Checks gym progression';

t.stage = q.player.data.gym_stage;

t.stage < 6 ? {
    return 'EASY';
};

t.stage < 12 && t.stage > 5 ? {
    return 'MEDIUM';
};

t.stage < 18 && t.stage > 11 ? {
    return 'HARD';
};

return 'UNKNOWN';